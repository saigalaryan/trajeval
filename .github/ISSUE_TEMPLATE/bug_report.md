---
name: Bug report
about: Something in trajeval doesn't work as documented
title: ""
labels: bug
---

**What happened**

A clear description of the bug.

**Minimal repro**

Ideally a golden dataset entry + adapter (or adapter output) that triggers
it — see `packages/trajeval/CONTRIBUTING.md`'s bug-reporting note. If it's a
web viewer issue, a RunResult JSON (redacted if needed) that reproduces it.

```python
# minimal adapter/config/etc.
```

**Expected behavior**

What you expected to happen instead.

**Environment**

- trajeval version: `trajeval --version`
- Python version:
- OS:
- (if web viewer) browser:

**Additional context**

Anything else relevant — full traceback, `trajeval run` output, etc.
